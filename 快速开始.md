# Light-Quant 快速开始指南

## 第一步：获取 ModelScope API Key

1. 访问 https://modelscope.cn/
2. 注册/登录账号
3. 进入「个人中心」→「API Key 管理」
4. 复制你的 API Key

## 第二步：配置环境变量

在项目根目录创建 `.env` 文件（复制自 `.env.example`）：

```env
# 必须配置 - 魔塔社区 API Key
MODELSCOPE_API_KEY=你的 API_Key_粘贴到这里

# 可选 - Bark 推送 (iOS)
BARK_TOKEN=你的 Bark_Token

# 可选 - Pushdeer 推送 (跨平台)
PUSHDEER_URL=https://pushdeer.cn/api/push/你的 Pushkey
```

## 第三步：安装依赖

```bash
pip install -r requirements.txt
```

## 第四步：运行测试（可选）

```bash
python test_system.py
```

## 第五步：启动服务

```bash
python main.py
```

访问 http://localhost:8000 查看看板。

## 手机访问

在同一局域网下，手机浏览器访问：
`http://你的电脑 IP:8000`

例如：`http://192.168.1.100:8000`

## 常用操作

1. **手动选股**: 点击「开始选股」按钮
2. **查看结果**: 自动刷新显示推荐股票
3. **定时任务**: 工作日 9:25 和 15:10 自动执行
