# Light-Quant v1.0 - 个人 AI 智能量化选股系统

> 基于本地 PC 部署的智能化 A 股筛选工具，结合传统量化因子与 LLM 语义分析。

---

## 📋 目录

1. [快速开始](#快速开始)
2. [环境配置](#环境配置)
3. [启动服务](#启动服务)
4. [PyCharm 配置](#pycharm-配置)
5. [测试说明](#测试说明)
6. [API 接口](#api-接口)
7. [定时任务](#定时任务)
8. [常见问题](#常见问题)

---

## ⚡ 快速开始

### 前提条件

- **Python 3.10+** (重要！必须使用 py311 环境)
- 网络连接 (访问数据源和 AI API)
- 1GB+ 可用内存

### 第一步：激活正确的 Python 环境

```bash
# Windows Anaconda 用户
conda activate py311

# 验证版本
python --version  # 应显示 Python 3.11.x
```

### 第二步：安装依赖

```bash
pip install -r requirements.txt
```

### 第三步：启动服务

```bash
# 方式 1: 使用启动脚本（推荐）
run_py311.bat run

# 方式 2: 直接使用 main.py
D:\Users\sjx\Anaconda\envs\py311\python.exe main.py

# 方式 3: 使用 startup.py
D:\Users\sjx\Anaconda\envs\py311\python.exe startup.py run
```

### 第四步：访问看板

浏览器打开：**http://localhost:8000/dashboard**

---

## 🔧 环境配置

### .env 配置文件

编辑项目根目录的 `.env` 文件：

```env
# ==================== 必需配置 ====================

# ModelScope API Key（AI 分析需要）
MODELSCOPE_API_KEY=你的_api_key_在这里

# ==================== 可选配置 ====================

# Bark 推送（iOS 设备通知）
BARK_TOKEN=你的_bark_token

# Pushdeer 推送（跨平台通知）
PUSHDEER_URL=https://pushdeer.cn/api/push/你的_pushkey

# ==================== 选股参数调整 ====================

# 候选股票数量
SCREEN_CANDIDATE_COUNT=20

# PE 上限（0-50 之间）
STOCK_FILTER_PE_MAX=50.0

# 换手率范围 (%)
STOCK_FILTER_TURNOVER_MIN=3.0
STOCK_FILTER_TURNOVER_MAX=10.0

# ==================== 网络请求设置 ====================

# 请求延迟（秒），避免被封 IP
REQUEST_DELAY_MIN=1.0
REQUEST_DELAY_MAX=2.0

# ==================== 服务配置 ====================

# Web 服务端口
APP_PORT=8000

# 日志级别
LOG_LEVEL=INFO
```

### 获取 ModelScope API Key

1. 访问 https://modelscope.cn/
2. 注册/登录账号
3. 进入「个人中心」→「API Key 管理」
4. 创建并复制 API Key

---

## 🚀 启动服务

### 推荐方式（Windows）

```batch
REM 检查环境
run_py311.bat startup.py check

REM 启动服务
run_py311.bat run

REM 运行测试
run_py311.bat run_tests.py
```

### Linux/Mac 方式

```bash
# 确保使用 Python 3.10+
source ~/anaconda3/bin/activate py311

# 检查环境
python startup.py check

# 启动服务
python startup.py run
```

### 服务地址

| 功能 | URL |
|------|-----|
| 选股看板 | http://localhost:8000/dashboard |
| API 文档 | http://localhost:8000/docs |
| ReDoc 文档 | http://localhost:8000/redoc |
| 健康检查 | http://localhost:8000/api/health |

### 手机远程访问

在同一局域网下，访问：

```
http://你的电脑 IP:8000/dashboard
```

例如：`http://192.168.1.100:8000`

---

## 💻 PyCharm 配置

### ⚠️ 重要提示

**不要使用**: `D:\Users\sjx\Anaconda\python.exe` (这是 Python 3.8)

**必须使用**: `D:\Users\sjx\Anaconda\envs\py311\python.exe` (Python 3.11)

### 步骤 1: 添加解释器

1. 打开 PyCharm
2. `File` → `Settings` → `Project: AIStockAnalysis` → `Python Interpreter`
3. 点击齿轮图标 ⚙️ → `Add...`
4. 选择 `Existing environment`
5. 浏览到：`D:\Users\sjx\Anaconda\envs\py311\python.exe`
6. 点击 `OK`

### 步骤 2: 验证配置

在 PyCharm 终端运行：

```python
import sys
print(sys.executable)  # 应显示 py311 路径
print(sys.version)     # 应显示 3.11.x
```

### 步骤 3: 配置运行参数

1. 点击右上角运行配置 → `Edit Configurations...`
2. 选择 `main.py` 配置
3. 确认 "Python interpreter" 是 py311
4. Working directory 设置为项目根目录

### 调试问题

如果仍然报错，运行检查脚本：

```python
# check_python.py
import sys
print(f"Python: {sys.executable}")
import pydantic
print(f"Pydantic: {pydantic.VERSION}")
print(f"Pydantic path: {pydantic.__file__}")
```

输出应该显示 py311 环境路径。

---

## 🧪 测试说明

### pytest 单元测试

```bash
# 运行所有测试
run_py311.bat run_tests.py

# 详细输出
run_py311.bat run_tests.py -v

# 跳过慢速测试
run_py311.bat run_tests.py --quick

# 生成覆盖率报告
run_py311.bat run_tests.py --cov --html
```

### 测试文件结构

```
tests/
├── test_models.py          # 数据模型测试 (18 用例)
├── test_indicators.py      # 技术指标测试 (20 用例)
├── test_helpers.py         # 工具函数测试 (27 用例)
├── test_ai_analyst.py      # AI 分析测试 (20 用例)
├── test_push_service.py    # 推送服务测试 (20 用例)
├── test_api.py             # API 接口测试 (16 用例)
├── test_stock_engine.py    # 选股引擎测试 (15 用例)
├── test_data_fetcher.py    # 数据采集测试 (19 用例)
└── test_scheduler.py       # 定时任务测试 (20 用例)
```

**总计**: ~175 个测试用例

### 测试分类

| 类型 | 测试文件 | 说明 |
|------|----------|------|
| 纯单元测试 | test_models.py, test_indicators.py, test_helpers.py | 无需外部依赖 |
| Mock 集成测试 | test_stock_engine.py, test_data_fetcher.py, test_scheduler.py, test_api.py | 模拟外部服务 |
| 需配置测试 | test_ai_analyst.py, test_push_service.py | 需要 API Key/Token |

---

## 🔌 API 接口

### 手动触发选股

```bash
curl -X POST http://localhost:8000/api/stocks/screen
```

响应：

```json
{
  "status": "started",
  "message": "Screening task has been triggered",
  "check_status": "/stocks/result"
}
```

### 获取选股结果

```bash
curl http://localhost:8000/api/stocks/result
```

响应示例：

```json
{
  "status": "success",
  "run_id": "20240328120000_abc123",
  "stats": {
    "total_stocks": 4500,
    "filtered_stocks": 3000,
    "candidate_stocks": 20,
    "final_picks": 5
  },
  "picks": [
    {
      "rank": 1,
      "symbol": "000001",
      "name": "平安银行",
      "price": 12.5,
      "change_percent": 2.3,
      "ai_score": 8.5,
      "ai_decision": "推荐",
      "ai_reason": "业绩良好，技术面突破"
    }
  ]
}
```

### 获取定时任务列表

```bash
curl http://localhost:8000/api/scheduler/jobs
```

---

## ⏰ 定时任务

### 默认配置

| 任务 | 时间 | 说明 |
|------|------|------|
| 盘前选股 | 工作日 9:25 | 集合竞价结束后 |
| 盘后复盘 | 工作日 15:10 | 收盘后 |

### 修改定时任务

编辑 `api/scheduler.py`:

```python
def _setup_jobs(self):
    # 盘前选股
    self.scheduler.add_job(
        self._run_morning_screening,
        CronTrigger(hour=9, minute=25, day_of_week="mon-fri"),
        id="morning_screening",
        name="盘前选股"
    )

    # 盘后复盘
    self.scheduler.add_job(
        self._run_evening_screening,
        CronTrigger(hour=15, minute=10, day_of_week="mon-fri"),
        id="evening_screening",
        name="盘后复盘"
    )
```

### 自定义示例

```python
# 每天上午 10:00 执行
CronTrigger(hour=10, minute=0)

# 每个交易日下午 3 点执行
CronTrigger(hour=15, minute=0, day_of_week="mon-fri")

# 每周一早上 9:00 执行
CronTrigger(hour=9, minute=0, day_of_week="mon")
```

---

## ❓ 常见问题

### Q1: ImportError: cannot import name 'Secret' from 'pydantic'

**原因**: 使用了错误的 Python 环境（Python 3.8）

**解决**:
1. 始终使用 py311 环境：`D:\Users\sjx\Anaconda\envs\py311\python.exe`
2. 不要在 PyCharm 中使用 `D:\Users\sjx\Anaconda\python.exe`
3. 使用 `run_py311.bat` 脚本

### Q2: ModelScope API Key 未配置

**现象**: AI 分析功能不可用

**解决**:
1. 在 `.env` 中配置 `MODELSCOPE_API_KEY`
2. 或从 https://modelscope.cn/ 获取

### Q3: AkShare 数据获取失败

**现象**: `Failed to fetch stock list`

**解决**:
1. 检查网络连接
2. 增加请求延迟：
   ```env
   REQUEST_DELAY_MIN=2.0
   REQUEST_DELAY_MAX=3.0
   ```
3. 更新 akshare: `pip install -U akshare`

### Q4: 端口被占用

**现象**: `Address already in use`

**解决**:
1. 修改 `.env`: `APP_PORT=8001`
2. 或关闭占用进程

### Q5: 推送不工作

**现象**: 选股完成后无推送

**解决**:
1. 检查 `.env` 中 BARK_TOKEN 或 PUSHDEER_URL
2. 查看日志确认状态
3. 推送是可选的，不影响选股功能

---

## 📁 项目结构

```
AIStockAnalysis/
├── main.py                 # 主入口
├── startup.py              # 快速启动脚本
├── run_py311.bat           # py311 环境启动脚本
├── run_tests.bat           # 测试运行脚本
├── check_python.py         # Python 环境检查脚本
├── .env                    # 环境配置
├── requirements.txt        # Python 依赖
│
├── config/                 # 配置模块
│   ├── settings.py         # 配置管理
│   └── models.py           # 数据模型
│
├── core/                   # 核心逻辑
│   ├── data_fetcher.py     # 数据采集 (AkShare)
│   ├── indicators.py       # 技术指标计算
│   ├── ai_analyst.py       # AI 股票分析师
│   ├── push_service.py     # 消息推送
│   └── stock_engine.py     # 选股引擎
│
├── api/                    # FastAPI 接口
│   ├── main.py             # 应用入口
│   ├── routes.py           # API 路由
│   └── scheduler.py        # 定时任务
│
├── utils/                  # 工具函数
│   ├── helpers.py          # 通用工具
│   └── logger.py           # 日志配置
│
├── tests/                  # pytest 单元测试
│   └── *.py                # 9 个测试文件
│
├── web/                    # Web 前端
│   ├── static/style.css
│   └── templates/dashboard.html
│
└── logs/                   # 日志文件
    └── light_quant_*.log
```

---

## 📊 选股逻辑

### 第一层：量化初筛

1. 股价 > 20 日均线
2. 换手率 3% ~ 10%
3. PE 0 ~ 50
4. 非 ST、非停牌
5. KDJ 金叉 / MACD 多头加分

### 第二层：AI 研判

1. 新闻情绪分析（利好/利空）
2. 公告风险识别（减持、诉讼等）
3. 综合评分排序（0-10 分）

### 评分标准

| 分数 | 决策 | 说明 |
|------|------|------|
| 9-10 | 推荐 | 重大利好，无明显风险 |
| 7-8 | 推荐 | 基本面良好，积极因素 |
| 5-6 | 观望 | 中性偏多，需观察 |
| 3-4 | 排除 | 存在一定风险 |
| 0-2 | 排除 | 重大利空或高风险 |

---

## 📝 日志查看

```bash
# Windows PowerShell
Get-Content logs/light_quant_*.log -Tail 50 -Wait

# Linux/Mac
tail -f logs/light_quant_*.log
```

---

## 🤝 技术支持

- README.md - 项目总览
- RUN_GUIDE.md - 详细运行指南
- tests/README.md - 测试套件文档
- PROJECT_COMPLETION_SUMMARY.md - 完成报告

---

*最后更新：2026-03-28 | 版本：v1.0*
